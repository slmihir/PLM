{
  "name": "virgio-plm",
  "version": "1.0.0",
  "private": true,
  "description": "VIRGIO PLM System - Phase 1",
  "scripts": {
    "dev": "pnpm --filter api dev & pnpm --filter web dev & pnpm --filter agent dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

